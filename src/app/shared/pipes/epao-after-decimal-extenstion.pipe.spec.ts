import { ePaoAfterDecimalExtenstionPipe } from './epao-after-decimal-extenstion.pipe';

describe('AfterDecimalExtenstionPipe', () => {
  it('create an instance', () => {
    const pipe = new ePaoAfterDecimalExtenstionPipe();
    expect(pipe).toBeTruthy();
  });
});
